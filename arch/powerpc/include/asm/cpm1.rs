//! Automatically rewritten from C Header to Rust Module
//! Source: arch/powerpc/include/asm/cpm1.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0
//
// MPC8xx Communication Processor Module.
// Copyright (c) 1997 Dan Malek (dmalek@jlc.net)
//
// This file contains structures and information for the communication
// processor channels.  Some CPM control and status is available
// through the MPC8xx internal memory map.  See immap.h for details.
// This file only contains what I need for the moment, not the total
// CPM capabilities.  I (or someone else) will add definitions as they
// are needed.  -- Dan
//
// On the MBX board, EPPC-Bug loads CPM microcode into the first 512
// bytes of the DP RAM and relocates the I2C parameter area to the
// IDMA1 space.  The remaining DP RAM is available for buffer descriptors
// or other use.
//

// CPM Command register.
//

// Channel numbers.
//

// Export the base address of the communication processor registers
// and dual port ram.
//
extern "C" {
    pub fn cpm_setbrg(brg: c_uint, rate: c_uint);
}
extern "C" {
    pub fn cpm_load_patch(cp: *mut cpm8xx_t) -> void __init;
}
extern "C" {
    pub fn cpm_reset();
}
// Parameter RAM offsets.
//

// Define enough so I can at least use the serial port as a UART.
// The MBX uses SMC1 as the host serial port.
//
// Function code bits.
//

// SMC uart mode register.
//

// SMC2 as Centronics parallel printer.  It is half duplex, in that
// it can only receive or transmit.  The parameter ram values for
// each direction are either unique or properly overlap, so we can
// include them in one structure.
//
// Centronics Status Mask Register.
//

// SMC Event and Mask register.
//

// Baud rate generators.
//

// SI Clock Route Register
//

// SCCs.
//

// SCC Event and Mask register.
//

// Function code bits.
//

// CPM Ethernet through SCCx.
//
// NOTE: Some versions of the manual have the following items
// incorrectly documented.  Below is the proper order.
//
// SCC Event register as used by Ethernet.
//

// SCC Mode Register (PMSR) as used by Ethernet.
//

// SCC as UART
//
// SCC Event and Mask registers when it is used as a UART.
//

// The SCC PMSR when used as a UART.
//

// CPM Transparent mode SCC.
//
// IIC parameter RAM.
//
// RISC Controller Configuration Register definitons
//
pub const RCCR_TIME: c_uint = 0x8000			/* RISC Timer Enable */;

pub const RCCR_TIME_MASK: c_uint = 0x00FF			/* not RISC Timer related bits */;
// RISC Timer Parameter RAM offset

// Bits in RISC Timer Command Register
pub const TM_CMD_VALID: c_uint = 0x80000000	/* Valid - Enables the timer */;
pub const TM_CMD_RESTART: c_uint = 0x40000000	/* Restart - for automatic restart */;
pub const TM_CMD_PWM: c_uint = 0x20000000	/* Run in Pulse Width Modulation Mode */;

// CPM interrupts.  There are nearly 32 interrupts generated by CPM
// channels or devices.  All of these are presented to the PPC core
// as a single interrupt.  The CPM interrupt handler dispatches its
// own handlers, in a similar fashion to the PPC core handler.  We
// use the table as defined in the manuals (i.e. no special high
// priority and SCC1 == SCCa, etc...).
//
pub const CPMVEC_NR: c_int = 32;

// CPM interrupt configuration vector.
//

pub const CPM_PIN_INPUT: c_int = 0;
pub const CPM_PIN_OUTPUT: c_int = 1;
pub const CPM_PIN_PRIMARY: c_int = 0;
pub const CPM_PIN_SECONDARY: c_int = 2;
pub const CPM_PIN_GPIO: c_int = 4;
pub const CPM_PIN_OPENDRAIN: c_int = 8;
pub const CPM_PIN_FALLEDGE: c_int = 16;
pub const CPM_PIN_ANYEDGE: c_int = 0;
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum cpm_port {
    CPM_PORTA,
    CPM_PORTB,
    CPM_PORTC,
    CPM_PORTD,
    CPM_PORTE,
}

extern "C" {
    pub fn cpm1_set_pin(port: cpm_port, pin: c_int, flags: c_int);
}
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum cpm_clk_dir {
    CPM_CLK_RX,
    CPM_CLK_TX,
    CPM_CLK_RTX
}

#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum cpm_clk_target {
    CPM_CLK_SCC1,
    CPM_CLK_SCC2,
    CPM_CLK_SCC3,
    CPM_CLK_SCC4,
    CPM_CLK_SMC1,
    CPM_CLK_SMC2,
}

#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum cpm_clk {
    CPM_BRG1,	/* Baud Rate Generator  1 */
    CPM_BRG2,	/* Baud Rate Generator  2 */
    CPM_BRG3,	/* Baud Rate Generator  3 */
    CPM_BRG4,	/* Baud Rate Generator  4 */
    CPM_CLK1,	/* Clock  1 */
    CPM_CLK2,	/* Clock  2 */
    CPM_CLK3,	/* Clock  3 */
    CPM_CLK4,	/* Clock  4 */
    CPM_CLK5,	/* Clock  5 */
    CPM_CLK6,	/* Clock  6 */
    CPM_CLK7,	/* Clock  7 */
    CPM_CLK8,	/* Clock  8 */
}

extern "C" {
    pub fn cpm1_clk_setup(target: cpm_clk_target, clock: c_int, mode: c_int) -> c_int;
}
extern "C" {
    pub fn cpm1_gpiochip_add16(dev: *mut device) -> c_int;
}
extern "C" {
    pub fn cpm1_gpiochip_add32(dev: *mut device) -> c_int;
}
