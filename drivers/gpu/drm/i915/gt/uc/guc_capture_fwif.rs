//! Automatically rewritten from C Header to Rust Module
//! Source: drivers/gpu/drm/i915/gt/uc/guc_capture_fwif.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: MIT
//
// Copyright © 2021-2022 Intel Corporation
//

//
// struct __guc_capture_bufstate
//
// Book-keeping structure used to track read and write pointers
// as we extract error capture data from the GuC-log-buffer's
// error-capture region as a stream of dwords.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct __guc_capture_bufstate {
    pub size: u32,
    pub data: *mut c_void,
    pub rd: u32,
    pub wr: u32,
}

//
// struct __guc_capture_parsed_output - extracted error capture node
//
// A single unit of extracted error-capture output data grouped together
// at an engine-instance level. We keep these nodes in a linked list.
// See cachelist and outlist below.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct __guc_capture_parsed_output {
//
// A single set of 3 capture lists: a global-list
// an engine-class-list and an engine-instance list.
// outlist in __guc_capture_parsed_output will keep
// a linked list of these nodes that will eventually
// be detached from outlist and attached into to
// i915_gpu_codedump in response to a context reset
//
    pub link: list_head,
    pub is_partial: bool,
    pub eng_class: u32,
    pub eng_inst: u32,
    pub guc_id: u32,
    pub lrca: u32,
#[repr(C)]
#[derive(Copy, Clone)]
pub struct gcap_reg_list_info {
    pub vfid: u32,
    pub num_regs: u32,
    pub regs: *mut guc_mmio_reg,
    pub reginfo: [}; GUC_CAPTURE_LIST_TYPE_MAX],
}

//
// struct guc_debug_capture_list_header / struct guc_debug_capture_list
//
// As part of ADS registration, these header structures (followed by
// an array of 'struct guc_mmio_reg' entries) are used to register with
// GuC microkernel the list of registers we want it to dump out prior
// to a engine reset.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_debug_capture_list_header {
    pub info: u32,

    pub __packed: },
#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_debug_capture_list {
    pub header: guc_debug_capture_list_header,
    pub regs: [guc_mmio_reg; ],
    pub __packed: },
//
// struct __guc_mmio_reg_descr / struct __guc_mmio_reg_descr_group
//
// intel_guc_capture module uses these structures to maintain static
// tables (per unique platform) that consists of lists of registers
// (offsets, names, flags,...) that are used at the ADS registration
// time as well as during runtime processing and reporting of error-
// capture states generated by GuC just prior to engine reset events.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct __guc_mmio_reg_descr {
    pub reg: i915_reg_t,
    pub flags: u32,
    pub mask: u32,
    pub regname: *const c_char,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __guc_mmio_reg_descr_group {
    pub list: *const __guc_mmio_reg_descr,
    pub num_regs: u32,
    pub /: *mut *mut u32 owner; / see enum guc_capture_owner,
    pub /: *mut *mut u32 type; / see enum guc_capture_type,
    pub /: *mut *mut u32 engine; / as per MAX_ENGINE_CLASS,
    pub /: *mut *mut *mut __guc_mmio_reg_descr extlist; / only used for steered registers,
}

//
// struct guc_state_capture_header_t / struct guc_state_capture_t
// guc_state_capture_group_header_t / guc_state_capture_group_t
//
// Prior to resetting engines that have hung or faulted, GuC microkernel
// reports the engine error-state (register values that was read) by
// logging them into the shared GuC log buffer using these hierarchy
// of structures.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_state_capture_header_t {
    pub owner: u32,

    pub info: u32,

    pub /: *mut *mut u32 lrca; / if type-instance, LRCA (address) that hung, else set to ~0,
    pub /: *mut *mut u32 guc_id; / if type-instance, context index of hung context, else set to ~0,
    pub num_mmios: u32,

    pub __packed: },
#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_state_capture_t {
    pub header: guc_state_capture_header_t,
    pub mmio_entries: [guc_mmio_reg; ],
    pub __packed: },
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum guc_capture_group_types {
    GUC_STATE_CAPTURE_GROUP_TYPE_FULL,
    GUC_STATE_CAPTURE_GROUP_TYPE_PARTIAL,
    GUC_STATE_CAPTURE_GROUP_TYPE_MAX,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_state_capture_group_header_t {
    pub owner: u32,

    pub info: u32,

    pub __packed: },
// this is the top level structure where an error-capture dump starts
#[repr(C)]
#[derive(Copy, Clone)]
pub struct guc_state_capture_group_t {
    pub grp_header: guc_state_capture_group_header_t,
    pub capture_entries: [guc_state_capture_t; ],
    pub __packed: },
//
// struct __guc_capture_ads_cache
//
// A structure to cache register lists that were populated and registered
// with GuC at startup during ADS registration. This allows much quicker
// GuC resets without re-parsing all the tables for the given gt.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct __guc_capture_ads_cache {
    pub is_valid: bool,
    pub ptr: *mut c_void,
    pub size: usize,
    pub status: c_int,
}

//
// struct intel_guc_state_capture
//
// Internal context of the intel_guc_capture module.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct intel_guc_state_capture {
//
// @reglists: static table of register lists used for error-capture state.
//
    pub reglists: *const __guc_mmio_reg_descr_group,
//
// @extlists: allocated table of steered register lists used for error-capture state.
//
// NOTE: steered registers have multiple instances depending on the HW configuration
// (slices or dual-sub-slices) and thus depends on HW fuses discovered at startup
//
    pub extlists: *mut __guc_mmio_reg_descr_group,
//
// @ads_cache: cached register lists that is ADS format ready
//
// @ads_null_cache: ADS null cache.
//
    pub ads_null_cache: *mut c_void,
//
// @cachelist: Pool of pre-allocated nodes for error capture output
//
// We need this pool of pre-allocated nodes because we cannot
// dynamically allocate new nodes when receiving the G2H notification
// because the event handlers for all G2H event-processing is called
// by the ct processing worker queue and when that queue is being
// processed, there is no absolute guarantee that we are not in the
// midst of a GT reset operation (which doesn't allow allocations).
//
    pub cachelist: list_head,

pub const PREALLOC_NODES_DEFAULT_NUMREGS: c_int = 64;
//
// @max_mmio_per_node: Max MMIO per node.
//
    pub max_mmio_per_node: c_int,
//
// @outlist: Pool of pre-allocated nodes for error capture output
//
// A linked list of parsed GuC error-capture output data before
// reporting with formatting via i915_gpu_coredump. Each node in this linked list shall
// contain a single engine-capture including global, engine-class and
// engine-instance register dumps as per guc_capture_parsed_output_node
//
    pub outlist: list_head,
}
