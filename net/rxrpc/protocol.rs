//! Automatically rewritten from C Header to Rust Module
//! Source: net/rxrpc/protocol.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0-or-later
// packet.h: Rx packet layout and definitions
//
// Copyright (C) 2002, 2007 Red Hat, Inc. All Rights Reserved.
// Written by David Howells (dhowells@redhat.com)
//
// on-the-wire Rx packet header
// - all multibyte fields should be in network byte order
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxrpc_wire_header {
    pub /: *mut *mut __be32 epoch; / client boot timestamp,
pub const RXRPC_RANDOM_EPOCH: c_uint = 0x80000000	/* Random if set, date-based if not */;
    pub /: *mut *mut __be32 cid; / connection and channel ID,

    pub /: *mut *mut __be32 callNumber; / call ID (0 for connection-level packets),
    pub /: *mut *mut __be32 seq; / sequence number of pkt in call stream,
    pub /: *mut *mut __be32 serial; / serial number of pkt sent to network,
    pub /: *mut *mut uint8_t type; / packet type,

    pub /: *mut *mut uint8_t flags; / packet flags,
pub const RXRPC_CLIENT_INITIATED: c_uint = 0x01		/* signifies a packet generated by a client */;
pub const RXRPC_REQUEST_ACK: c_uint = 0x02		/* request an unconditional ACK of this packet */;
pub const RXRPC_LAST_PACKET: c_uint = 0x04		/* the last packet from this side for this call */;
pub const RXRPC_MORE_PACKETS: c_uint = 0x08		/* more packets to come */;
pub const RXRPC_JUMBO_PACKET: c_uint = 0x20		/* [DATA] this is a jumbo packet */;
pub const RXRPC_SLOW_START_OK: c_uint = 0x20		/* [ACK] slow start supported */;
    pub /: *mut *mut uint8_t userStatus; / app-layer defined status,
pub const RXRPC_USERSTATUS_SERVICE_UPGRADE: c_uint = 0x01	/* AuriStor service upgrade request */;
    pub /: *mut *mut uint8_t securityIndex; / security protocol ID,
    pub /: *mut *mut __be16 _rsvd; / reserved,
    pub /: *mut *mut __be16 cksum; / kerberos security checksum,
}

//
// jumbo packet secondary header
// - can be mapped to read header by:
// - new_serial = serial + 1
// - new_seq = seq + 1
// - new_flags = j_flags
// - new__rsvd = j__rsvd
// - duplicating all other fields
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxrpc_jumbo_header {
    pub /: *mut *mut uint8_t flags; / packet flags (as per rxrpc_header),
    pub pad: u8,
    pub /: *mut *mut __be16 _rsvd; / reserved,
    pub /: *mut *mut __be16 cksum; / kerberos security checksum,
}

//
// The maximum number of subpackets that can possibly fit in a UDP packet is:
//
// (max_UDP - wirehdr + jumbohdr) / (jumbohdr + 1412)
// = ((65535 - 28 + 4) / 1416)
// = 45 non-terminal packets and 1 terminal packet.
//
pub const RXRPC_MAX_NR_JUMBO: c_int = 46;
// Size of a jumbo packet with N subpackets, excluding UDP+IP

//
// on-the-wire Rx ACK packet data payload
// - all multibyte fields should be in network byte order
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxrpc_ackpacket {
    pub /: *mut *mut __be16 bufferSpace; / number of packet buffers available,
    pub no: *mut *mut __be16 maxSkew; / diff between serno being ACK'd and highest serial,
// received
    pub /: *mut *mut __be32 firstPacket; / sequence no of first ACK'd packet in attached list,
    pub /: *mut *mut __be32 previousPacket; / sequence no of previous packet received,
    pub /: *mut *mut __be32 serial; / serial no of packet that prompted this ACK,
    pub /: *mut *mut uint8_t reason; / reason for ACK,

    pub /: *mut *mut uint8_t nAcks; / number of ACKs,
pub const RXRPC_MAXACKS: c_int = 255;
    pub /: *mut *mut uint8_t acks[]; / list of ACK/NAKs,
pub const RXRPC_ACK_TYPE_NACK: c_int = 0;
pub const RXRPC_ACK_TYPE_ACK: c_int = 1;
    pub __packed: },
//
// ACK packets can have a further piece of information tagged on the end
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxrpc_acktrailer {
    pub /: *mut *mut __be32 maxMTU; / maximum Rx MTU size (bytes) [AFS 3.3],
    pub /: *mut *mut __be32 ifMTU; / maximum interface MTU size (bytes) [AFS 3.3],
    pub /: *mut *mut __be32 rwind; / Rx window size (packets) [AFS 3.4],
    pub /: *mut *mut __be32 jumbo_max; / max packets to stick into a jumbo packet [AFS 3.5],
}

//
// Kerberos security type-2 challenge packet
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxkad_challenge {
    pub /: *mut *mut __be32 version; / version of this challenge type,
    pub /: *mut *mut __be32 nonce; / encrypted random number,
    pub /: *mut *mut __be32 min_level; / minimum security level,
    pub /: *mut *mut __be32 __padding; / padding to 8-byte boundary,
    pub __packed: },
//
// Kerberos security type-2 response packet
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxkad_response {
    pub /: *mut *mut __be32 version; / version of this response type,
    pub __pad: __be32,
// encrypted bit of the response
    pub /: *mut *mut __be32 epoch; / current epoch,
    pub /: *mut *mut __be32 cid; / parent connection ID,
    pub /: *mut *mut __be32 checksum; / checksum,
    pub /: *mut *mut __be32 securityIndex; / security type,
    pub /: *mut *mut __be32 call_id[4]; / encrypted call IDs,
    pub /: *mut *mut __be32 inc_nonce; / challenge nonce + 1,
    pub /: *mut *mut __be32 level; / desired level,
    pub encrypted: },
    pub /: *mut *mut __be32 kvno; / Kerberos key version number,
    pub /: *mut *mut __be32 ticket_len; / Kerberos ticket length,
    pub __packed: },
//
// GSSAPI security type-4 and type-6 data header.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxgk_header {
    pub epoch: __be32,
    pub cid: __be32,
    pub call_number: __be32,
    pub seq: __be32,
    pub sec_index: __be32,
    pub data_len: __be32,
    pub __packed: },
//
// GSSAPI security type-4 and type-6 response packet header.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rxgk_response {
    pub start_time: __be64,
    pub token_len: __be32,
    pub __packed: },
