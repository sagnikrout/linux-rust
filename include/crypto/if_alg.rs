//! Automatically rewritten from C Header to Rust Module
//! Source: include/crypto/if_alg.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0-or-later
//
// if_alg: User-space algorithm interface
//
// Copyright (c) 2010 Herbert Xu <herbert@gondor.apana.org.au>
//

pub const ALG_MAX_PAGES: c_int = 16;
#[repr(C)]
#[derive(Copy, Clone)]
pub struct alg_sock {
// struct sock must be the first member of struct alg_sock
    pub sk: sock,
    pub parent: *mut sock,
    pub refcnt: core::sync::atomic::AtomicI32,
    pub nokey_refcnt: core::sync::atomic::AtomicI32,
    pub type: *const af_alg_type,
    pub private: *mut c_void,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_control {
    pub iv: *mut af_alg_iv,
    pub op: c_int,
    pub aead_assoclen: c_uint,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_type {
    pub name): *const *const *const void (bind)(char,
    pub private): *mut *mut void (release)(void,
    pub keylen): *const *const *const *const int (setkey)(void private, u8 key, unsigned int,
    pub len): *mut *mut *mut int (setentropy)(void private, sockptr_t entropy, unsigned int,
    pub sk): *mut *mut *mut int (accept)(void private, struct sock,
    pub sk): *mut *mut *mut int (accept_nokey)(void private, struct sock,
    pub authsize): *mut *mut *mut int (setauthsize)(void private, unsigned int,
    pub ops: *mut proto_ops,
    pub ops_nokey: *mut proto_ops,
    pub owner: *mut module,
    pub name: [c_char; 14],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_sgl {
    pub sgt: sg_table,
    pub 1]: scatterlist sgl[ALG_MAX_PAGES +,
    pub need_unpin: bool,
}

// TX SGL entry
#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_tsgl {
    pub list: list_head,
    pub /: *mut *mut unsigned int cur; / Last processed SG entry,
    pub /: *mut *mut scatterlist sg[]; / Array of SGs forming the SGL,
}

// RX SGL entry
#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_rsgl {
    pub sgl: af_alg_sgl,
    pub list: list_head,
    pub /: *mut *mut size_t sg_num_bytes; / Bytes of data in that SGL,
}

//
// struct af_alg_async_req - definition of crypto request
// @sk:			Socket the request is associated with
// @first_rsgl:		First RX SG
// @last_rsgl:		Pointer to last RX SG
// @rsgl_list:		Track RX SGs
// @tsgl:		Private, per request TX SGL of buffers to process
// @tsgl_entries:	Number of entries in priv. TX SGL
// @outlen:		Number of output bytes generated by crypto op
// @areqlen:		Length of this data structure
// @cra_u:		Cipher request
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_async_req {
    pub sk: *mut sock,
    pub first_rsgl: af_alg_rsgl,
    pub last_rsgl: *mut af_alg_rsgl,
    pub rsgl_list: list_head,
    pub tsgl: *mut scatterlist,
    pub tsgl_entries: c_uint,
    pub outlen: c_uint,
    pub areqlen: c_uint,
    pub aead_req: aead_request,
    pub skcipher_req: skcipher_request,
    pub cra_u: },
// req ctx trails this struct
}

//
// struct af_alg_ctx - definition of the crypto context
//
// The crypto context tracks the input data during the lifetime of an AF_ALG
// socket.
//
// @tsgl_list:		Link to TX SGL
// @iv:			IV for cipher operation
// @state:		Existing state for continuing operation
// @aead_assoclen:	Length of AAD for AEAD cipher operations
// @wait:		For waiting for completion of async crypto ops
// @used:		TX bytes sent to kernel. This variable is used to
// ensure that user space cannot cause the kernel
// to allocate too much memory in sendmsg operation.
// @rcvused:		Total RX bytes to be filled by kernel. This variable
// is used to ensure user space cannot cause the kernel
// to allocate too much memory in a recvmsg operation.
// @more:		More data to be expected from user space?
// @merge:		Shall new data from user space be merged into existing
// SG?
// @enc:		Cryptographic operation to be performed when
// recvmsg is invoked.
// @write:		True if we are in the middle of a write.
// @init:		True if metadata has been sent.
// @len:		Length of memory allocated for this data structure.
// @inflight:		Non-zero when requests are in flight, for debugging only.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_ctx {
    pub tsgl_list: list_head,
    pub iv: *mut c_void,
    pub state: *mut c_void,
    pub aead_assoclen: usize,
    pub wait: crypto_wait,
    pub used: usize,
    pub rcvused: core::sync::atomic::AtomicI32,
    pub len: c_uint,
    pub inflight: c_uint,
}

// Flags for af_alg_allowlist_entry::flags:

#[repr(C)]
#[derive(Copy, Clone)]
pub struct af_alg_allowlist_entry {
    pub name: *const c_char,
    pub flags: u32,
}

extern "C" {
    pub fn af_alg_register_type(type: *const af_alg_type) -> c_int;
}
extern "C" {
    pub fn af_alg_unregister_type(type: *const af_alg_type) -> c_int;
}
extern "C" {
    pub fn af_alg_release(sock: *mut socket) -> c_int;
}
extern "C" {
    pub fn af_alg_release_parent(sk: *mut sock);
}
extern "C" {
    pub fn af_alg_free_sg(sgl: *mut af_alg_sgl);
}
//
// af_alg_sndbuf - Size of available buffer for sending data from user space to kernel.
//
// @sk: socket of connection to user space
//
// Returns: number of bytes still available
//
// af_alg_writable - Can the send buffer still be written to?
//
// @sk: socket of connection to user space
//
// Returns: true => writable, false => not writable
//
// af_alg_rcvbuf - Size of available buffer used by kernel for the RX user space operation.
//
// @sk: socket of connection to user space
//
// Returns: number of bytes still available
//
// af_alg_readable - Can the RX buffer still be read from?
//
// @sk: socket of connection to user space
//
// Returns: true => readable, false => not readable
//
extern "C" {
    pub fn af_alg_count_tsgl(sk: *mut sock, bytes: usize) -> c_uint;
}
extern "C" {
    pub fn af_alg_pull_tsgl(sk: *mut sock, used: usize, dst: *mut scatterlist);
}
extern "C" {
    pub fn af_alg_wmem_wakeup(sk: *mut sock);
}
extern "C" {
    pub fn af_alg_wait_for_data(sk: *mut sock, flags: unsigned, min: unsigned) -> c_int;
}
extern "C" {
    pub fn af_alg_free_resources(areq: *mut af_alg_async_req);
}
//
// Mask used to disable unsupported algorithm implementations.
//
// This is the same as FSCRYPT_CRYPTOAPI_MASK in fs/crypto/fscrypt_private.h.
// In additions to the motivations there, this API is exposed to userspace
// that might not be fully trusted.
//

