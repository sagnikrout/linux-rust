//! Automatically rewritten from C Header to Rust Module
//! Source: include/linux/mfd/wm8994/pdata.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0-or-later
//
// include/linux/mfd/wm8994/pdata.h -- Platform data for WM8994
//
// Copyright 2009 Wolfson Microelectronics PLC.
//
// Author: Mark Brown <broonie@opensource.wolfsonmicro.com>
//
pub const WM8994_NUM_LDO: c_int = 2;
pub const WM8994_NUM_GPIO: c_int = 11;
pub const WM8994_NUM_AIF: c_int = 3;
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8994_ldo_pdata {
    pub init_data: *const regulator_init_data,
}

pub const WM8994_CONFIGURE_GPIO: c_uint = 0x10000;
pub const WM8994_DRC_REGS: c_int = 5;
pub const WM8994_EQ_REGS: c_int = 20;
pub const WM8958_MBC_CUTOFF_REGS: c_int = 20;
pub const WM8958_MBC_COEFF_REGS: c_int = 48;
pub const WM8958_MBC_COMBINED_REGS: c_int = 56;
pub const WM8958_VSS_HPF_REGS: c_int = 2;
pub const WM8958_VSS_REGS: c_int = 148;
pub const WM8958_ENH_EQ_REGS: c_int = 32;
//
// DRC configurations are specified with a label and a set of register
// values to write (the enable bits will be ignored).  At runtime an
// enumerated control will be presented for each DRC block allowing
// the user to choose the configuration to use.
//
// Configurations may be generated by hand or by using the DRC control
// panel provided by the WISCE - see  http://www.wolfsonmicro.com/wisce
// for details.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8994_drc_cfg {
    pub name: *const c_char,
    pub regs: [u16; WM8994_DRC_REGS],
}

//
// ReTune Mobile configurations are specified with a label, sample
// rate and set of values to write (the enable bits will be ignored).
//
// Configurations are expected to be generated using the ReTune Mobile
// control panel in WISCE - see http://www.wolfsonmicro.com/wisce
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8994_retune_mobile_cfg {
    pub name: *const c_char,
    pub rate: c_uint,
    pub regs: [u16; WM8994_EQ_REGS],
}

//
// Multiband compressor configurations are specified with a label and
// two sets of values to write.  Configurations are expected to be
// generated using the multiband compressor configuration panel in
// WISCE - see http://www.wolfsonmicro.com/wisce
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8958_mbc_cfg {
    pub name: *const c_char,
    pub cutoff_regs: [u16; WM8958_MBC_CUTOFF_REGS],
    pub coeff_regs: [u16; WM8958_MBC_COEFF_REGS],
// Coefficient layout when using MBC+VSS firmware
    pub combined_regs: [u16; WM8958_MBC_COMBINED_REGS],
}

//
// VSS HPF configurations are specified with a label and two values to
// write.  Configurations are expected to be generated using the
// multiband compressor configuration panel in WISCE - see
// http://www.wolfsonmicro.com/wisce
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8958_vss_hpf_cfg {
    pub name: *const c_char,
    pub regs: [u16; WM8958_VSS_HPF_REGS],
}

//
// VSS configurations are specified with a label and array of values
// to write.  Configurations are expected to be generated using the
// multiband compressor configuration panel in WISCE - see
// http://www.wolfsonmicro.com/wisce
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8958_vss_cfg {
    pub name: *const c_char,
    pub regs: [u16; WM8958_VSS_REGS],
}

//
// Enhanced EQ configurations are specified with a label and array of
// values to write.  Configurations are expected to be generated using
// the multiband compressor configuration panel in WISCE - see
// http://www.wolfsonmicro.com/wisce
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8958_enh_eq_cfg {
    pub name: *const c_char,
    pub regs: [u16; WM8958_ENH_EQ_REGS],
}

//
// Microphone detection rates, used to tune response rates and power
// consumption for WM8958/WM1811 microphone detection.
//
// @sysclk: System clock rate to use this configuration for.
// @idle: True if this configuration should use when no accessory is detected,
// false otherwise.
// @start: Value for MICD_BIAS_START_TIME register field (not shifted).
// @rate: Value for MICD_RATE register field (not shifted).
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8958_micd_rate {
    pub sysclk: c_int,
    pub idle: bool,
    pub start: c_int,
    pub rate: c_int,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wm8994_pdata {
    pub gpio_base: c_int,
//
// Default values for GPIOs if non-zero, WM8994_CONFIGURE_GPIO
// can be used for all zero values.
//
    pub gpio_defaults: [c_int; WM8994_NUM_GPIO],
    pub ldo: [wm8994_ldo_pdata; WM8994_NUM_LDO],
    pub /: *mut *mut *mut int irq_base; / Base IRQ number for WM8994, required for IRQs,
    pub /: *mut *mut *mut unsigned long irq_flags; / user irq flags,
    pub num_drc_cfgs: c_int,
    pub drc_cfgs: *mut wm8994_drc_cfg,
    pub num_retune_mobile_cfgs: c_int,
    pub retune_mobile_cfgs: *mut wm8994_retune_mobile_cfg,
    pub num_mbc_cfgs: c_int,
    pub mbc_cfgs: *mut wm8958_mbc_cfg,
    pub num_vss_cfgs: c_int,
    pub vss_cfgs: *mut wm8958_vss_cfg,
    pub num_vss_hpf_cfgs: c_int,
    pub vss_hpf_cfgs: *mut wm8958_vss_hpf_cfg,
    pub num_enh_eq_cfgs: c_int,
    pub enh_eq_cfgs: *mut wm8958_enh_eq_cfg,
    pub num_micd_rates: c_int,
    pub micd_rates: *mut wm8958_micd_rate,
// Power up delays to add after microphone bias power up (ms)
    pub micb1_delay: c_int,
    pub micb2_delay: c_int,
// LINEOUT can be differential or single ended
    pub lineout1_diff:1: c_uint,
    pub lineout2_diff:1: c_uint,
// Common mode feedback
    pub lineout1fb:1: c_uint,
    pub lineout2fb:1: c_uint,
// Delay between detecting a jack and starting microphone
// detect (specified in ms)
//
    pub micdet_delay: c_int,
// Delay between microphone detect completing and reporting on
// insert (specified in ms)
//
    pub mic_id_delay: c_int,
// IRQ for microphone detection if brought out directly as a
// signal.
//
    pub micdet_irq: c_int,
// WM8994 microphone biases: 0=0.9*AVDD1 1=0.65*AVVD1
    pub micbias1_lvl:1: c_uint,
    pub micbias2_lvl:1: c_uint,
// WM8994 jack detect threashold levels, see datasheet for values
    pub jd_scthr:2: c_uint,
    pub jd_thr:2: c_uint,
// Configure WM1811 jack detection for use with external capacitor
    pub jd_ext_cap:1: c_uint,
// WM8958 microphone bias configuration
    pub micbias: [c_int; 2],
// WM8958 microphone detection ranges
    pub micd_lvl_sel: u16,
// Disable the internal pull downs on the LDOs if they are
// always driven (eg, connected to an always on supply or
// GPIO that always drives an output.  If they float power
// consumption will rise.
//
    pub ldo_ena_always_driven: bool,
//
// SPKMODE must be pulled internally by the device on this
// system.
//
    pub spkmode_pu: bool,
//
// CS/ADDR must be pulled internally by the device on this
// system.
//
    pub csnaddr_pd: bool,
//
// Maximum number of channels clocks will be generated for,
// useful for systems where and I2S bus with multiple data
// lines is mastered.
//
    pub max_channels_clocked: [c_int; WM8994_NUM_AIF],
}
