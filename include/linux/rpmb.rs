//! Automatically rewritten from C Header to Rust Module
//! Source: include/linux/rpmb.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0
//
// Copyright (C) 2015-2019 Intel Corp. All rights reserved
// Copyright (C) 2021-2022 Linaro Ltd
//

//
// enum rpmb_type - type of underlying storage technology
//
// @RPMB_TYPE_EMMC  : emmc (JESD84-B50.1)
// @RPMB_TYPE_UFS   : UFS (JESD220)
// @RPMB_TYPE_NVME  : NVM Express
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum rpmb_type {
    RPMB_TYPE_EMMC,
    RPMB_TYPE_UFS,
    RPMB_TYPE_NVME,
}

//
// struct rpmb_descr - RPMB description provided by the underlying block device
//
// @type             : block device type
// @route_frames     : routes frames to and from the RPMB device
// @dev_id           : unique device identifier read from the hardware
// @dev_id_len       : length of unique device identifier
// @reliable_wr_count: number of sectors that can be written in one access
// @capacity         : capacity of the device in units of 128K
//
// @dev_id is intended to be used as input when deriving the authenticaion key.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rpmb_descr {
    pub type: rpmb_type,
    pub resp_len): *mut *mut u8 resp, unsigned int,
    pub dev_id: *mut u8,
    pub dev_id_len: usize,
    pub reliable_wr_count: u16,
    pub capacity: u16,
}

//
// struct rpmb_dev - device which can support RPMB partition
//
// @dev              : device
// @id               : device_id
// @list_node        : linked list node
// @descr            : RPMB description
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rpmb_dev {
    pub dev: device,
    pub id: c_int,
    pub list_node: list_head,
    pub descr: rpmb_descr,
}

//
// struct rpmb_frame - RPMB frame structure for authenticated access
//
// @stuff        : stuff bytes, a padding/reserved area of 196 bytes at the
// beginning of the RPMB frame. They don’t carry meaningful
// data but are required to make the frame exactly 512 bytes.
// @key_mac      : The authentication key or the message authentication
// code (MAC) depending on the request/response type.
// The MAC will be delivered in the last (or the only)
// block of data.
// @data         : Data to be written or read by signed access.
// @nonce        : Random number generated by the host for the requests
// and copied to the response by the RPMB engine.
// @write_counter: Counter value for the total amount of the successful
// authenticated data write requests made by the host.
// @addr         : Address of the data to be programmed to or read
// from the RPMB. Address is the serial number of
// the accessed block (half sector 256B).
// @block_count  : Number of blocks (half sectors, 256B) requested to be
// read/programmed.
// @result       : Includes information about the status of the write counter
// (valid, expired) and result of the access made to the RPMB.
// @req_resp     : Defines the type of request and response to/from the memory.
//
// The stuff bytes and big-endian properties are modeled to fit to the spec.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct rpmb_frame {
    pub stuff: [u8; 196],
    pub key_mac: [u8; 32],
    pub data: [u8; 256],
    pub nonce: [u8; 16],
    pub write_counter: __be32,
    pub addr: __be16,
    pub block_count: __be16,
    pub result: __be16,
    pub req_resp: __be16,
}

pub const RPMB_PROGRAM_KEY: c_uint = 0x1    /* Program RPMB Authentication Key */;
pub const RPMB_GET_WRITE_COUNTER: c_uint = 0x2    /* Read RPMB write counter */;
pub const RPMB_WRITE_DATA: c_uint = 0x3    /* Write data to RPMB partition */;
pub const RPMB_READ_DATA: c_uint = 0x4    /* Read data from RPMB partition */;
pub const RPMB_RESULT_READ: c_uint = 0x5    /* Read result request  (Internal) */;

extern "C" {
    pub fn rpmb_dev_put(rdev: *mut rpmb_dev);
}
extern "C" {
    pub fn rpmb_interface_register(intf: *mut class_interface) -> c_int;
}
extern "C" {
    pub fn rpmb_interface_unregister(intf: *mut class_interface);
}
extern "C" {
    pub fn rpmb_dev_unregister(rdev: *mut rpmb_dev) -> c_int;
}

