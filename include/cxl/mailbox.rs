//! Automatically rewritten from C Header to Rust Module
//! Source: include/cxl/mailbox.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: GPL-2.0-only
// Copyright(c) 2024 Intel Corporation.

//
// struct cxl_mbox_cmd - A command to be submitted to hardware.
// @opcode: (input) The command set and command submitted to hardware.
// @payload_in: (input) Pointer to the input payload.
// @payload_out: (output) Pointer to the output payload. Must be allocated by
// the caller.
// @size_in: (input) Number of bytes to load from @payload_in.
// @size_out: (input) Max number of bytes loaded into @payload_out.
// (output) Number of bytes generated by the device. For fixed size
// outputs commands this is always expected to be deterministic. For
// variable sized output commands, it tells the exact number of bytes
// written.
// @min_out: (input) internal command output payload size validation
// @poll_count: (input) Number of timeouts to attempt.
// @poll_interval_ms: (input) Time between mailbox background command polling
// interval timeouts.
// @return_code: (output) Error code returned from hardware.
//
// This is the primary mechanism used to send commands to the hardware.
// All the fields except @payload_* correspond exactly to the fields described in
// Command Register section of the CXL 2.0 8.2.8.4.5. @payload_in and
// @payload_out are written to, and read from the Command Payload Registers
// defined in CXL 2.0 8.2.8.4.8.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct cxl_mbox_cmd {
    pub opcode: u16,
    pub payload_in: *mut c_void,
    pub payload_out: *mut c_void,
    pub size_in: usize,
    pub size_out: usize,
    pub min_out: usize,
    pub poll_count: c_int,
    pub poll_interval_ms: c_int,
    pub return_code: u16,
}

//
// struct cxl_mailbox - context for CXL mailbox operations
// @host: device that hosts the mailbox
// @enabled_cmds: mailbox commands that are enabled by the driver
// @exclusive_cmds: mailbox commands that are exclusive to the kernel
// @payload_size: Size of space for payload
// (CXL 3.1 8.2.8.4.3 Mailbox Capabilities Register)
// @mbox_mutex: mutex protects device mailbox and firmware
// @feat_mutex: serializes multi-part Get/Set Feature transfers
// @mbox_wait: rcuwait for mailbox
// @mbox_send: @dev specific transport for transmitting mailbox commands
// @feat_cap: Features capability
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct cxl_mailbox {
    pub host: *mut device,
    pub CXL_MEM_COMMAND_ID_MAX): DECLARE_BITMAP(enabled_cmds,,
    pub CXL_MEM_COMMAND_ID_MAX): DECLARE_BITMAP(exclusive_cmds,,
    pub payload_size: usize,
    pub /: *mut *mut mutex mbox_mutex; / lock to protect mailbox context,
    pub feat_mutex: mutex,
    pub mbox_wait: rcuwait,
    pub cmd): *mut *mut *mut int (mbox_send)(struct cxl_mailbox cxl_mbox, struct cxl_mbox_cmd,
    pub feat_cap: cxl_features_capability,
}

extern "C" {
    pub fn cxl_mailbox_init(cxl_mbox: *mut cxl_mailbox, host: *mut device) -> c_int;
}
