//! Automatically rewritten from C Header to Rust Module
//! Source: include/uapi/linux/batadv_packet.h
#![no_std]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_mut)]

use core::ffi::*;

// --- Linux Kernel Primitives Prelude ---
pub type uid_t = u32;
pub type gid_t = u32;
pub type uid16_t = u16;
pub type gid16_t = u16;
pub type pid_t = i32;
pub type mode_t = u32;
pub type umode_t = u16;
pub type nlink_t = u32;
pub type off_t = i64;
pub type loff_t = i64;
pub type dev_t = u32;
pub type ino_t = u64;
pub type size_t = usize;
pub type ssize_t = isize;
pub type uintptr_t = usize;
pub type intptr_t = isize;
pub type ptrdiff_t = isize;
pub type clockid_t = i32;
pub type timer_t = i32;
pub type time64_t = i64;
pub type atomic_t = core::sync::atomic::AtomicI32;
pub type atomic64_t = core::sync::atomic::AtomicI64;
// ---------------------------------------


// SPDX-License-Identifier: (GPL-2.0 WITH Linux-syscall-note)
// Copyright (C) B.A.T.M.A.N. contributors:
//
// Marek Lindner, Simon Wunderlich
//

//
// batadv_tp_is_error() - Check throughput meter return code for error
// @n: throughput meter return code
//
// Return: 0 when not error was detected, != 0 otherwise
//

//
// enum batadv_packettype - types for batman-adv encapsulated packets
// @BATADV_IV_OGM: originator messages for B.A.T.M.A.N. IV
// @BATADV_BCAST: broadcast packets carrying broadcast payload
// @BATADV_CODED: network coded packets
// @BATADV_ELP: echo location packets for B.A.T.M.A.N. V
// @BATADV_OGM2: originator messages for B.A.T.M.A.N. V
// @BATADV_MCAST: multicast packet with multiple destination addresses
//
// @BATADV_UNICAST: unicast packets carrying unicast payload traffic
// @BATADV_UNICAST_FRAG: unicast packets carrying a fragment of the original
// payload packet
// @BATADV_UNICAST_4ADDR: unicast packet including the originator address of
// the sender
// @BATADV_ICMP: unicast packet like IP ICMP used for ping or traceroute
// @BATADV_UNICAST_TVLV: unicast packet carrying TVLV containers
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_packettype {
// 0x00 - 0x3f: local packets or special rules for handling
    BATADV_IV_OGM           = 0x00,
    BATADV_BCAST            = 0x01,
    BATADV_CODED            = 0x02,
    BATADV_ELP		= 0x03,
    BATADV_OGM2		= 0x04,
    BATADV_MCAST            = 0x05,
// 0x40 - 0x7f: unicast
pub const BATADV_UNICAST_MIN: c_uint = 0x40;
    BATADV_UNICAST          = 0x40,
    BATADV_UNICAST_FRAG     = 0x41,
    BATADV_UNICAST_4ADDR    = 0x42,
    BATADV_ICMP             = 0x43,
    BATADV_UNICAST_TVLV     = 0x44,
pub const BATADV_UNICAST_MAX: c_uint = 0x7f;
// 0x80 - 0xff: reserved
}

//
// enum batadv_subtype - packet subtype for unicast4addr
// @BATADV_P_DATA: user payload
// @BATADV_P_DAT_DHT_GET: DHT request message
// @BATADV_P_DAT_DHT_PUT: DHT store message
// @BATADV_P_DAT_CACHE_REPLY: ARP reply generated by DAT
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_subtype {
    BATADV_P_DATA			= 0x01,
    BATADV_P_DAT_DHT_GET		= 0x02,
    BATADV_P_DAT_DHT_PUT		= 0x03,
    BATADV_P_DAT_CACHE_REPLY	= 0x04,
}

// this file is included by batctl which needs these defines
pub const BATADV_COMPAT_VERSION: c_int = 15;
//
// enum batadv_iv_flags - flags used in B.A.T.M.A.N. IV OGM packets
// @BATADV_NOT_BEST_NEXT_HOP: flag is set when the ogm packet is forwarded and
// was previously received from someone other than the best neighbor.
// @BATADV_PRIMARIES_FIRST_HOP: flag unused.
// @BATADV_DIRECTLINK: flag is for the first hop or if rebroadcasted from a
// one hop neighbor on the interface where it was originally received.
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_iv_flags {
    BATADV_NOT_BEST_NEXT_HOP   = 1UL << 0,
    BATADV_PRIMARIES_FIRST_HOP = 1UL << 1,
    BATADV_DIRECTLINK          = 1UL << 2,
}

//
// enum batadv_icmp_packettype - ICMP message types
// @BATADV_ECHO_REPLY: success reply to BATADV_ECHO_REQUEST
// @BATADV_DESTINATION_UNREACHABLE: failure when route to destination not found
// @BATADV_ECHO_REQUEST: request BATADV_ECHO_REPLY from destination
// @BATADV_TTL_EXCEEDED: error after BATADV_ECHO_REQUEST traversed too many hops
// @BATADV_PARAMETER_PROBLEM: return code for malformed messages
// @BATADV_TP: throughput meter packet
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_icmp_packettype {
    BATADV_ECHO_REPLY	       = 0,
    BATADV_DESTINATION_UNREACHABLE = 3,
    BATADV_ECHO_REQUEST	       = 8,
    BATADV_TTL_EXCEEDED	       = 11,
    BATADV_PARAMETER_PROBLEM       = 12,
    BATADV_TP		       = 15,
}

//
// enum batadv_mcast_flags - flags for multicast capabilities and settings
// @BATADV_MCAST_WANT_ALL_UNSNOOPABLES: we want all packets destined for
// 224.0.0.0/24 or ff02::1
// @BATADV_MCAST_WANT_ALL_IPV4: we want all IPv4 multicast packets
// (both link-local and routable ones)
// @BATADV_MCAST_WANT_ALL_IPV6: we want all IPv6 multicast packets
// (both link-local and routable ones)
// @BATADV_MCAST_WANT_NO_RTR4: we have no IPv4 multicast router and therefore
// only need routable IPv4 multicast packets we signed up for explicitly
// @BATADV_MCAST_WANT_NO_RTR6: we have no IPv6 multicast router and therefore
// only need routable IPv6 multicast packets we signed up for explicitly
// @BATADV_MCAST_HAVE_MC_PTYPE_CAPA: we can parse, receive and forward
// batman-adv multicast packets with a multicast tracker TVLV. And all our
// hard interfaces have an MTU of at least 1280 bytes.
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_mcast_flags {
    BATADV_MCAST_WANT_ALL_UNSNOOPABLES	= 1UL << 0,
    BATADV_MCAST_WANT_ALL_IPV4		= 1UL << 1,
    BATADV_MCAST_WANT_ALL_IPV6		= 1UL << 2,
    BATADV_MCAST_WANT_NO_RTR4		= 1UL << 3,
    BATADV_MCAST_WANT_NO_RTR6		= 1UL << 4,
    BATADV_MCAST_HAVE_MC_PTYPE_CAPA		= 1UL << 5,
}

// tt data subtypes
pub const BATADV_TT_DATA_TYPE_MASK: c_uint = 0x0F;
//
// enum batadv_tt_data_flags - flags for tt data tvlv
// @BATADV_TT_OGM_DIFF: TT diff propagated through OGM
// @BATADV_TT_REQUEST: TT request message
// @BATADV_TT_RESPONSE: TT response message
// @BATADV_TT_FULL_TABLE: contains full table to replace existing table
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_tt_data_flags {
    BATADV_TT_OGM_DIFF   = 1UL << 0,
    BATADV_TT_REQUEST    = 1UL << 1,
    BATADV_TT_RESPONSE   = 1UL << 2,
    BATADV_TT_FULL_TABLE = 1UL << 4,
}

//
// enum batadv_vlan_flags - flags for the four MSB of any vlan ID field
// @BATADV_VLAN_HAS_TAG: whether the field contains a valid vlan tag or not
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_vlan_flags {
    BATADV_VLAN_HAS_TAG	= 1UL << 15,
}

//
// enum batadv_bla_claimframe - claim frame types for the bridge loop avoidance
// @BATADV_CLAIM_TYPE_CLAIM: claim of a client mac address
// @BATADV_CLAIM_TYPE_UNCLAIM: unclaim of a client mac address
// @BATADV_CLAIM_TYPE_ANNOUNCE: announcement of backbone with current crc
// @BATADV_CLAIM_TYPE_REQUEST: request of full claim table
// @BATADV_CLAIM_TYPE_LOOPDETECT: mesh-traversing loop detect packet
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_bla_claimframe {
    BATADV_CLAIM_TYPE_CLAIM		= 0x00,
    BATADV_CLAIM_TYPE_UNCLAIM	= 0x01,
    BATADV_CLAIM_TYPE_ANNOUNCE	= 0x02,
    BATADV_CLAIM_TYPE_REQUEST	= 0x03,
    BATADV_CLAIM_TYPE_LOOPDETECT	= 0x04,
}

//
// enum batadv_tvlv_type - tvlv type definitions
// @BATADV_TVLV_GW: gateway tvlv
// @BATADV_TVLV_DAT: distributed arp table tvlv
// @BATADV_TVLV_NC: network coding tvlv
// @BATADV_TVLV_TT: translation table tvlv
// @BATADV_TVLV_ROAM: roaming advertisement tvlv
// @BATADV_TVLV_MCAST: multicast capability tvlv
// @BATADV_TVLV_MCAST_TRACKER: multicast tracker tvlv
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_tvlv_type {
    BATADV_TVLV_GW			= 0x01,
    BATADV_TVLV_DAT			= 0x02,
    BATADV_TVLV_NC			= 0x03,
    BATADV_TVLV_TT			= 0x04,
    BATADV_TVLV_ROAM		= 0x05,
    BATADV_TVLV_MCAST		= 0x06,
    BATADV_TVLV_MCAST_TRACKER	= 0x07,
}

//
// struct batadv_bla_claim_dst - layout of the destination MAC of a BLA claim
// frame
// @magic: fixed magic prefix (FF:43:05) identifying claim frames
// @type: claim frame type, see &enum batadv_bla_claimframe
// @group: group identifier of the announcing backbone gateway
//
// used in the destination hardware field of the ARP frame
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_bla_claim_dst {
    pub magic: [__u8; 3],
    pub type: __u8,
    pub group: __be16,
}

//
// struct batadv_ogm_packet - ogm (routing protocol) packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @flags: contains routing relevant flags - see enum batadv_iv_flags
// @seqno: sequence identification
// @orig: address of the source node
// @prev_sender: address of the previous sender
// @reserved: reserved byte for alignment
// @tq: transmission quality
// @tvlv_len: length of tvlv data following the ogm header
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_ogm_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub flags: __u8,
    pub seqno: __be32,
    pub orig: [__u8; ETH_ALEN],
    pub prev_sender: [__u8; ETH_ALEN],
    pub reserved: __u8,
    pub tq: __u8,
    pub tvlv_len: __be16,
}

//
// struct batadv_ogm2_packet - ogm2 (routing protocol) packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @flags: reserved for routing relevant flags - currently always 0
// @seqno: sequence number
// @orig: originator mac address
// @tvlv_len: length of the appended tvlv buffer (in bytes)
// @throughput: the currently flooded path throughput
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_ogm2_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub flags: __u8,
    pub seqno: __be32,
    pub orig: [__u8; ETH_ALEN],
    pub tvlv_len: __be16,
    pub throughput: __be32,
}

//
// struct batadv_elp_packet - elp (neighbor discovery) packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @orig: originator mac address
// @seqno: sequence number
// @elp_interval: currently used ELP sending interval in ms
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_elp_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub orig: [__u8; ETH_ALEN],
    pub seqno: __be32,
    pub elp_interval: __be32,
}

//
// struct batadv_icmp_header - common members among all the ICMP packets
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @msg_type: ICMP packet type
// @dst: address of the destination node
// @orig: address of the source node
// @uid: local ICMP socket identifier
// @align: not used - useful for alignment purposes only
//
// This structure is used for ICMP packet parsing only and it is never sent
// over the wire. The alignment field at the end is there to ensure that
// members are padded the same way as they are in real packets.
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_icmp_header {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub /: *mut *mut __u8 msg_type; / see ICMP message types above,
    pub dst: [__u8; ETH_ALEN],
    pub orig: [__u8; ETH_ALEN],
    pub uid: __u8,
    pub align: [__u8; 3],
}

//
// struct batadv_icmp_packet - ICMP packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @msg_type: ICMP packet type
// @dst: address of the destination node
// @orig: address of the source node
// @uid: local ICMP socket identifier
// @reserved: not used - useful for alignment
// @seqno: ICMP sequence number
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_icmp_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub /: *mut *mut __u8 msg_type; / see ICMP message types above,
    pub dst: [__u8; ETH_ALEN],
    pub orig: [__u8; ETH_ALEN],
    pub uid: __u8,
    pub reserved: __u8,
    pub seqno: __be16,
}

//
// struct batadv_icmp_tp_packet - ICMP TP Meter packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @msg_type: ICMP packet type
// @dst: address of the destination node
// @orig: address of the source node
// @uid: local ICMP socket identifier
// @subtype: TP packet subtype (see batadv_icmp_tp_subtype)
// @session: TP session identifier
// @seqno: the TP sequence number
// @timestamp: time when the packet has been sent. This value is filled in a
// TP_MSG and echoed back in the next TP_ACK so that the sender can compute the
// RTT. Since it is read only by the host which wrote it, there is no need to
// store it using network order
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_icmp_tp_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub /: *mut *mut __u8 msg_type; / see ICMP message types above,
    pub dst: [__u8; ETH_ALEN],
    pub orig: [__u8; ETH_ALEN],
    pub uid: __u8,
    pub subtype: __u8,
    pub session: [__u8; 2],
    pub seqno: __be32,
    pub timestamp: __be32,
}

//
// enum batadv_icmp_tp_subtype - ICMP TP Meter packet subtypes
// @BATADV_TP_MSG: Msg from sender to receiver
// @BATADV_TP_ACK: acknowledgment from receiver to sender
//
#[repr(C)]
#[derive(Copy, Clone, PartialEq, Eq)]
pub enum batadv_icmp_tp_subtype {
    BATADV_TP_MSG	= 0,
    BATADV_TP_ACK,
}

pub const BATADV_RR_LEN: c_int = 16;
//
// struct batadv_icmp_packet_rr - ICMP RouteRecord packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @msg_type: ICMP packet type
// @dst: address of the destination node
// @orig: address of the source node
// @uid: local ICMP socket identifier
// @rr_cur: number of entries the rr array
// @seqno: ICMP sequence number
// @rr: route record array
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_icmp_packet_rr {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub /: *mut *mut __u8 msg_type; / see ICMP message types above,
    pub dst: [__u8; ETH_ALEN],
    pub orig: [__u8; ETH_ALEN],
    pub uid: __u8,
    pub rr_cur: __u8,
    pub seqno: __be16,
    pub rr: [__u8; BATADV_RR_LEN][ETH_ALEN],
}

// All packet headers in front of an ethernet header have to be completely
// divisible by 2 but not by 4 to make the payload after the ethernet
// header again 4 bytes boundary aligned.
//
// A packing of 2 is necessary to avoid extra padding at the end of the struct
// caused by a structure member which is larger than two bytes. Otherwise
// the structure would not fulfill the previously mentioned rule to avoid the
// misalignment of the payload after the ethernet header. It may also lead to
// leakage of information when the padding it not initialized before sending.
//
// struct batadv_unicast_packet - unicast packet for network payload
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @ttvn: translation table version number
// @dest: originator destination of the unicast packet
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_unicast_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub /: *mut *mut __u8 ttvn; / destination translation table version number,
    pub dest: [__u8; ETH_ALEN],
// "4 bytes boundary + 2 bytes" long to make the payload after the
// following ethernet header again 4 bytes boundary aligned
//
}

//
// struct batadv_unicast_4addr_packet - extended unicast packet
// @u: common unicast packet header
// @src: address of the source
// @subtype: packet subtype
// @reserved: reserved byte for alignment
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_unicast_4addr_packet {
    pub u: batadv_unicast_packet,
    pub src: [__u8; ETH_ALEN],
    pub subtype: __u8,
    pub reserved: __u8,
// "4 bytes boundary + 2 bytes" long to make the payload after the
// following ethernet header again 4 bytes boundary aligned
//
}

//
// struct batadv_frag_packet - fragmented packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @dest: final destination used when routing fragments
// @orig: originator of the fragment used when merging the packet
// @no: fragment number within this sequence
// @priority: priority of frame, from ToS IP precedence or 802.1p
// @reserved: reserved byte for alignment
// @seqno: sequence identification
// @total_size: size of the merged packet
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_frag_packet {
    pub packet_type: __u8,
    pub /: *mut *mut __u8 version; / batman version field,
    pub ttl: __u8,

    pub no:4: __u8,
    pub priority:3: __u8,
    pub reserved:1: __u8,

    pub reserved:1: __u8,
    pub priority:3: __u8,
    pub no:4: __u8,
    pub dest: [__u8; ETH_ALEN],
    pub orig: [__u8; ETH_ALEN],
    pub seqno: __be16,
    pub total_size: __be16,
}

//
// struct batadv_bcast_packet - broadcast packet for network payload
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @reserved: reserved byte for alignment
// @seqno: sequence identification
// @orig: originator of the broadcast packet
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_bcast_packet {
    pub packet_type: __u8,
    pub /: *mut *mut __u8 version; / batman version field,
    pub ttl: __u8,
    pub reserved: __u8,
    pub seqno: __be32,
    pub orig: [__u8; ETH_ALEN],
// "4 bytes boundary + 2 bytes" long to make the payload after the
// following ethernet header again 4 bytes boundary aligned
//
}

//
// struct batadv_mcast_packet - multicast packet for network payload
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @reserved: reserved byte for alignment
// @tvlv_len: length of the appended tvlv buffer (in bytes)
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_mcast_packet {
    pub packet_type: __u8,
    pub version: __u8,
    pub ttl: __u8,
    pub reserved: __u8,
    pub tvlv_len: __be16,
// "4 bytes boundary + 2 bytes" long to make the payload after the
// following ethernet header again 4 bytes boundary aligned
//
}

//
// struct batadv_coded_packet - network coded packet
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @first_ttvn: tt-version number of first included packet
// @first_source: original source of first included packet
// @first_orig_dest: original destination of first included packet
// @first_crc: checksum of first included packet
// @second_ttl: ttl of second packet
// @second_ttvn: tt version number of second included packet
// @second_dest: second receiver of this coded packet
// @second_source: original source of second included packet
// @second_orig_dest: original destination of second included packet
// @second_crc: checksum of second included packet
// @coded_len: length of network coded part of the payload
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_coded_packet {
    pub packet_type: __u8,
    pub /: *mut *mut __u8 version; / batman version field,
    pub ttl: __u8,
    pub first_ttvn: __u8,
// __u8 first_dest[ETH_ALEN]; - saved in mac header destination
    pub first_source: [__u8; ETH_ALEN],
    pub first_orig_dest: [__u8; ETH_ALEN],
    pub first_crc: __be32,
    pub second_ttl: __u8,
    pub second_ttvn: __u8,
    pub second_dest: [__u8; ETH_ALEN],
    pub second_source: [__u8; ETH_ALEN],
    pub second_orig_dest: [__u8; ETH_ALEN],
    pub second_crc: __be32,
    pub coded_len: __be16,
}

//
// struct batadv_unicast_tvlv_packet - generic unicast packet with tvlv payload
// @packet_type: batman-adv packet type, part of the general header
// @version: batman-adv protocol version, part of the general header
// @ttl: time to live for this packet, part of the general header
// @reserved: reserved field (for packet alignment)
// @dst: address of the destination
// @src: address of the source
// @tvlv_len: length of tvlv data following the unicast tvlv header
// @align: 2 bytes to align the header to a 4 byte boundary
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_unicast_tvlv_packet {
    pub packet_type: __u8,
    pub /: *mut *mut __u8 version; / batman version field,
    pub ttl: __u8,
    pub reserved: __u8,
    pub dst: [__u8; ETH_ALEN],
    pub src: [__u8; ETH_ALEN],
    pub tvlv_len: __be16,
    pub align: __u16,
}

//
// struct batadv_tvlv_hdr - base tvlv header struct
// @type: tvlv container type (see batadv_tvlv_type)
// @version: tvlv container version
// @len: tvlv container length
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_hdr {
    pub type: __u8,
    pub version: __u8,
    pub len: __be16,
}

//
// struct batadv_tvlv_gateway_data - gateway data propagated through gw tvlv
// container
// @bandwidth_down: advertised uplink download bandwidth
// @bandwidth_up: advertised uplink upload bandwidth
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_gateway_data {
    pub bandwidth_down: __be32,
    pub bandwidth_up: __be32,
}

//
// struct batadv_tvlv_tt_vlan_data - vlan specific tt data propagated through
// the tt tvlv container
// @crc: crc32 checksum of the entries belonging to this vlan
// @vid: vlan identifier
// @reserved: unused, useful for alignment purposes
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_tt_vlan_data {
    pub crc: __be32,
    pub vid: __be16,
    pub reserved: __u16,
}

//
// struct batadv_tvlv_tt_data - tt data propagated through the tt tvlv container
// @flags: translation table flags (see batadv_tt_data_flags)
// @ttvn: translation table version number
// @num_vlan: number of announced VLANs. In the TVLV this struct is followed by
// one batadv_tvlv_tt_vlan_data object per announced vlan
// @vlan_data: array of batadv_tvlv_tt_vlan_data objects
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_tt_data {
    pub flags: __u8,
    pub ttvn: __u8,
    pub num_vlan: __be16,
    pub __counted_by_be(num_vlan): batadv_tvlv_tt_vlan_data vlan_data[],
}

//
// struct batadv_tvlv_tt_change - translation table diff data
// @flags: status indicators concerning the non-mesh client (see
// batadv_tt_client_flags)
// @reserved: reserved field - useful for alignment purposes only
// @addr: mac address of non-mesh client that triggered this tt change
// @vid: VLAN identifier
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_tt_change {
    pub flags: __u8,
    pub reserved: [__u8; 3],
    pub addr: [__u8; ETH_ALEN],
    pub vid: __be16,
}

//
// struct batadv_tvlv_roam_adv - roaming advertisement
// @client: mac address of roaming client
// @vid: VLAN identifier
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_roam_adv {
    pub client: [__u8; ETH_ALEN],
    pub vid: __be16,
}

//
// struct batadv_tvlv_mcast_data - payload of a multicast tvlv
// @flags: multicast flags announced by the orig node
// @reserved: reserved field
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_mcast_data {
    pub flags: __u8,
    pub reserved: [__u8; 3],
}

//
// struct batadv_tvlv_mcast_tracker - payload of a multicast tracker tvlv
// @num_dests: number of subsequent destination originator MAC addresses
//
#[repr(C)]
#[derive(Copy, Clone)]
pub struct batadv_tvlv_mcast_tracker {
    pub num_dests: __be16,
}

